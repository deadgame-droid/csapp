#include <limits.h>
#include <stdio.h>

int main() {
    for (int i = INT_MIN; i <= INT_MAX; i++) {
        for (int j = INT_MIN; j <= INT_MAX; j++) {
            unsigned ui = i;
            unsigned uj = j;
        }
    }
}
